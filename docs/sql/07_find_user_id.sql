-- =====================================================================
-- 07_find_user_id.sql — หา user id ของตัวเอง
--
-- รันหลังสมัครสมาชิกผ่านหน้าเว็บแล้วเท่านั้น (หรือ Dashboard >
-- Authentication > Add user) — ถ้ายังไม่เคยสมัคร ตารางนี้จะว่างเปล่า
--
-- Copy ค่า id (UUID) ของแถวที่ตรงกับ email ตัวเอง ไปใช้ต่อใน
-- 08_promote_admin.sql
-- =====================================================================

select id, email from auth.users;
