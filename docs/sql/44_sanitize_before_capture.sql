-- =================================================================
-- 44. ล้างเบอร์จริงก่อนถ่ายภาพหน้าจอลงรายงาน
--
-- บัญชีที่เพื่อนสมัครทดสอบระหว่างพัฒนา ใช้เบอร์มือถือจริงของตัวเอง
-- เบอร์พวกนี้แสดงในหน้า /admin/centers ส่วนรายชื่อผู้ใช้
-- ถ้าถ่ายภาพหน้านั้นลงรายงานที่ส่งอาจารย์ เบอร์จริงจะติดไปด้วย
--
-- เปลี่ยนเป็นเบอร์ตัวอย่างรูปแบบเดียวกับบัญชีอื่นในระบบ
-- ไม่กระทบการใช้งาน เพราะเบอร์ในโปรไฟล์เป็นข้อมูลติดต่อเฉย ๆ
-- ไม่ได้ใช้ยืนยันตัวตนหรือเข้าสู่ระบบ
-- =================================================================

update public.profiles set phone = '081-234-5685' where username = 'M3enSan';
update public.profiles set phone = '081-234-5686' where username = 'nteam';

-- ตรวจผล: ทุกบัญชีต้องเป็นรูปแบบ 08X-XXX-XXXX
select username, full_name, phone
  from public.profiles
 where phone is not null
 order by phone;
